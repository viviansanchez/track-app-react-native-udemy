import React from "react";
import { View, StyleSheet, Text } from "react-native";

const SigninScreen = () => {
  return <Text style={{ fontSize: 40 }}>SigninScreen</Text>;
};

const style = StyleSheet.create({});

export default SigninScreen;
