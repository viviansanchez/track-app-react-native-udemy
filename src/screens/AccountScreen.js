import React from "react";
import { View, StyleSheet, Text } from "react-native";

const AccountScreen = () => {
  return <Text style={{ fontSize: 40 }}>AccountScreen</Text>;
};

const style = StyleSheet.create({});

export default AccountScreen;
